name: CI

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    name: ${{ matrix.os }} - ${{ matrix.build_type }}
    runs-on: ${{ matrix.os }}
    
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        build_type: [Debug, Release]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up build environment (Ubuntu)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y cmake g++
    
    - name: Set up build environment (macOS)
      if: runner.os == 'macOS'
      run: |
        brew install cmake
    
    - name: Set up build environment (Windows)
      if: runner.os == 'Windows'
      uses: ilammy/msvc-dev-cmd@v1
    
    - name: Cache CMake build
      uses: actions/cache@v4
      with:
        path: |
          build
          ~/.cache/cmake
        key: ${{ runner.os }}-cmake-${{ matrix.build_type }}-${{ hashFiles('**/CMakeLists.txt') }}
        restore-keys: |
          ${{ runner.os }}-cmake-${{ matrix.build_type }}-
          ${{ runner.os }}-cmake-
    
    - name: Configure CMake
      run: cmake -B build -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} -DBUILD_TESTING=ON
    
    - name: Build
      run: cmake --build build --config ${{ matrix.build_type }} -j 4
    
    - name: Run tests
      run: ctest --test-dir build --build-config ${{ matrix.build_type }} --output-on-failure
    
    - name: Upload artifacts (executables)
      uses: actions/upload-artifact@v4
      with:
        name: binaries-${{ matrix.os }}-${{ matrix.build_type }}
        path: |
          build/bin/*
          build/${{ matrix.build_type }}/bin/*
        if-no-files-found: warn
    
    - name: Upload artifacts (libraries)
      uses: actions/upload-artifact@v4
      with:
        name: libraries-${{ matrix.os }}-${{ matrix.build_type }}
        path: |
          build/lib/*
          build/${{ matrix.build_type }}/lib/*
        if-no-files-found: warn

  sanitizer-checks:
    name: Sanitizers - ${{ matrix.sanitizer }}
    runs-on: ubuntu-latest
    
    strategy:
      fail-fast: false
      matrix:
        sanitizer: [asan, ubsan, asan+ubsan]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up build environment
      run: |
        sudo apt-get update
        sudo apt-get install -y cmake g++
    
    - name: Configure CMake (AddressSanitizer)
      if: matrix.sanitizer == 'asan'
      run: cmake -B build -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTING=ON -DENABLE_ASAN=ON
    
    - name: Configure CMake (UndefinedBehaviorSanitizer)
      if: matrix.sanitizer == 'ubsan'
      run: cmake -B build -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTING=ON -DENABLE_UBSAN=ON
    
    - name: Configure CMake (ASan + UBSan)
      if: matrix.sanitizer == 'asan+ubsan'
      run: cmake -B build -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTING=ON -DENABLE_ASAN=ON -DENABLE_UBSAN=ON
    
    - name: Build
      run: cmake --build build --config Debug -j 4
    
    - name: Run tests with sanitizers
      run: ctest --test-dir build --build-config Debug --output-on-failure
      env:
        ASAN_OPTIONS: detect_leaks=1:check_initialization_order=1
        UBSAN_OPTIONS: print_stacktrace=1